# ── Trained model files (large, go to S3/server — never to git) ──
models/*.pkl
models/*.csv
models/*.npy

# ── Jupyter notebook outputs ──────────────────────────────────────
model_training/*.ipynb
.ipynb_checkpoints/

# ── Python runtime ────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.DS_Store
